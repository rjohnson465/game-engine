/// ei_unfairy_add_ringedcoif

hatSpriteIndex = spr_player_ringed_coif;
ds_map_replace(defenses,SLASH,10);
ds_map_replace(defenses,CRUSH,5);
ds_map_replace(defenses,PIERCE,15);

poiseCurrent += 18;
poiseMax += 18;

hitSound = snd_metalhit1;
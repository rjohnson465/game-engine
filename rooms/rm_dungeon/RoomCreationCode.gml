room_speed = 30;


/*
for (var i = 0; i < 2; i++) {

	var portal = instance_create_depth(100,100,400, obj_portal);
	with portal {
		nextRoom = rm_dungeon2;
		nextRoomX = x; 
		nextRoomY = y;
		nextRoomLayer = "instances_floor_1";
		nextRoomName = getRoomName(nextRoom);
	}

}
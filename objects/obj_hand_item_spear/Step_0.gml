/// @description Insert description here
// You can write your code in this editor

// Inherit the parent event
event_inherited();

/*
if owner.lockOnTarget != noone && instance_exists(owner.lockOnTarget) {
	var dist = point_distance(owner.x,owner.y,owner.lockOnTarget.x,owner.lockOnTarget.y);
	show_debug_message("dist: " + string(dist));
}